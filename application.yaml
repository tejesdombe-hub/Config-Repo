server:
  port: 8082

spring:
  application:
    name: OrderService

  datasource:
    url: jdbc:mysql://localhost:3306/order_db
    username: root
    password: Admin@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka